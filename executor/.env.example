# mandatory
ORONYX_PACKAGE_ID=
ORONYX_OPERATOR_KEY=
ORONYX_MOCK_POOL_ID=

# Enoki gas station — PRIVATE key from the Enoki Portal (separate from the
# public key used on the frontend for zkLogin wallet registration). Must
# never be exposed to the browser; only used server-side in src/enoki.ts.
ORONYX_ENOKI_PRIVATE_API_KEY=

# testCetusSwap
ORONYX_OPERATOR_ADDR=
